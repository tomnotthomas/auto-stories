.env
node_modules/
.claude/
.vscode/

# Build + test artifacts (NestJS backend)
dist/
coverage/
*.tsbuildinfo

# Local eval fixtures — sample photos for the single-call quality eval
# (apps/api/scripts/eval-single-call). Kept out of git so the repo stays light;
# point EVAL_FIXTURES_DIR at your own folder to run it.
sample-story-photos/

# OpenAPI build artifact (regenerate with `npm run openapi:bundle`)
openapi/openapi.bundled.yaml
__pycache__/
.venv/
.DS_Store

# gcloud SDK installs (in case anyone unpacks it inside the repo)
.gcloud/
google-cloud-sdk/
google-cloud-cli-*.tar.gz
.gstack/

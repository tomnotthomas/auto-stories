# Rename to .env and fill in any keys your solution needs.
# We have accounts with these providers and will supply real keys when reviewing.
#
# Auto Stories only needs GOOGLE_CLOUD_API_KEY (the Gemini story engine).
# Everything else runs without a key; the app just can't generate a story until it's set.

ANTHROPIC_API_KEY=sk-ant-xxxxx
OPENAI_API_KEY=sk-xxxxx
ELEVEN_LABS_API_KEY=xxxxx
# Use the API key from Google AI Studio: https://aistudio.google.com/apikey
GOOGLE_CLOUD_API_KEY=xxxxx
AWS_ACCESS_KEY_ID=xxxxx
AWS_SECRET_ACCESS_KEY=xxxxx
AWS_REGION=us-west-2

# ── Fair use ────────────────────────────────────────────────────────────────
# Defaults match the Gemini free tier we run on: 20 model calls a day, 5 a
# minute. Leave them unset unless the tier changes — the defaults are the tier.
# DAILY_GENERATION_CAP=20   # model calls per day (not stories: a retry is a call)
# CALLS_PER_MINUTE=5        # model calls per minute; a burst waits, it does not fail
# RATE_LIMIT_PER_DAY=2      # stories one visitor may start per day
#
# GENERATION_TIMEOUT_MS=60000

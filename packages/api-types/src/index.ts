/**
 * @auto-stories/api-types — the shared, typed API contract.
 *
 * Generated from `openapi/openapi.yaml` (the source of truth) by kubb and
 * imported by both the Angular frontend and the NestJS backend, so a contract
 * change breaks compilation on both sides instead of failing at runtime.
 *
 * `./gen` is auto-generated (do not edit). This file is the stable public
 * surface — the one place to adjust if the generator is ever swapped.
 */
export * from "./gen/models";

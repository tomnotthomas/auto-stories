/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/


export type TextBlockFontEnum = "inter" | "playfair" | "space-mono" | "caveat";

export type TextBlockWeightEnum = "regular" | "bold";

export type TextBlockCaseEnum = "normal" | "upper";

export type TextBlockAlignEnum = "left" | "center" | "right";

export type TextBlockSizeEnum = "s" | "m" | "l";

export type TextBlockPositionEnum = "top-left" | "top-center" | "top-right" | "bottom-left" | "bottom-center" | "bottom-right";

/**
 * @description One piece of caption text placed on the frame. A frame carries 0–2 blocks (usually 0–1 — let the photo breathe): a small line and a bigger line, each with its own text, style, and spot. The client computes text colour and any background/scrim for readability (decision 7.10) and lets the user toggle it — the model never chooses those.
*/
export type TextBlock = {
    /**
     * @description The words to show — short, often just one or two.
     * @minLength 1
     * @type string
    */
    text: string;
    /**
     * @type string
    */
    font: TextBlockFontEnum;
    /**
     * @type string
    */
    weight: TextBlockWeightEnum;
    /**
     * @type string
    */
    case: TextBlockCaseEnum;
    /**
     * @type string
    */
    align: TextBlockAlignEnum;
    /**
     * @description This block\'s own size, so a small line and a bigger line can share a frame.
     * @type string
    */
    size: TextBlockSizeEnum;
    /**
     * @description Anchor zone (2 rows x 3 columns), kept off the subject in the middle.
     * @type string
    */
    position: TextBlockPositionEnum;
};
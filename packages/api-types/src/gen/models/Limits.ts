/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/


/**
 * @description What the caller has left under the shared free tier\'s fair-use guardrails\n(approach 4.1). Read before generating so the client can say what is left\n*before* the user picks photos and writes their line, rather than only after\na refusal (decision 7.36). Reading this does not count against any limit.\n
*/
export type Limits = {
    /**
     * @description Stories this caller can still start today.
     * @minLength 0
     * @type integer
    */
    remaining: number;
    /**
     * @description Stories one caller may start per day. Sent so the client never hardcodes a number the server owns.
     * @minLength 1
     * @type integer
    */
    limit: number;
    /**
     * @description When the caller\'s day rolls over and `remaining` returns to `limit`.
     * @type string, date-time
    */
    resetAt: string;
    /**
     * @description The *shared* daily budget is spent, so nobody can generate until it\nresets — regardless of what `remaining` says. Deliberately a flag and not\na count: it is not this caller\'s allowance, and a number they cannot act\non would only read as a scoreboard.\n
     * @type boolean
    */
    dayExhausted: boolean;
    /**
     * @description When the shared daily budget resets. Present only when `dayExhausted` is true.
     * @type string | undefined, date-time
    */
    dayResetAt?: string;
};